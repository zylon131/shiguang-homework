# 首版验收记录

验证日期：2026-09-15。仅使用本项目的虚构学生与合成作业图片；未使用其他项目的数据或实现。

## 已完成验证

- `npm run build`：TypeScript 检查、Vite 生产构建通过。
- `npm test`：10 组测试通过，覆盖机构隔离、会话、请求来源校验、学生分配、照片权限、上传幂等、队列批改、练习生成与编辑、周报统计、分享访问码、撤销、邀请单次使用以及保守判定和返回格式兼容。新增三科 18 份年级列表逐一与原始 tags 比对、Prompt 注入、非标准及跨年级标签拦截、「待归类」不计薄弱点等检查。
- `scripts/browser-check.js`：桌面、390px 手机和360px手机页面检查通过；集中确认、练习查看与修改、报告分享、家长访问、后台刷新保留输入焦点均通过，未发现浏览器运行异常或横向溢出。
- `scripts/upload-ui-check.js`：手机照片压缩、上传失败保留照片、重试复用幂等键、切换到同一学生的其他科目、一键准备11位已交作业学生的周报通过。该项使用测试网络拦截，不消耗模型额度。
- 初版真实 MiniMax-M3 调用（接入标准大纲之前）：语数英3张合成照片、6道题，识别出3道正确和3道错题；确认后生成9道练习，并生成及分享学情报告，流程用时43.7秒。历史证据保存在 `test-results/live/result.json`。
- 标准大纲接入后，真实 MiniMax-M3 数学三年级、英语三年级、语文一年级合成题测试通过；三科均识别 2 题及 1 道错题，并返回对应年级的原文标签。证据在 `test-results/curriculum-live/result.json`（数学/英语）与 `result-语文.json`。
- 公网 `https://8.130.50.140` 验收通过：可信 HTTPS、HTTP 跳转、生产禁用体验、Secure/HttpOnly 会话、三科 18 份列表与本地逐项一致、机构及照片隔离。真实上传数学合成照片得到 2 道题，手机确认后生成 3 道标准知识点练习，周报统计、家长访问码及分享撤销通过。该合成样例端到端 29.642 秒，不能代表整班耗时。证据在 `test-results/production/result.json`。
- 公网手机浏览器 390px、360px 检查通过，知识点下拉框的选项与三年级数学大纲完全一致，家长报告无横向溢出，无浏览器运行异常。截图位于 `test-results/production/`。验收中修正了测试脚本的手机入口与下拉框定位，无需更改产品流程。
- nginx 配置检查通过；Let’s Encrypt IP 证书自动续期演练通过，已启用每日两次续期检查的 systemd timer。
- 公网验收结束后已清理 6 个临时验收机构及 3 张合成照片；数据库完整性检查返回 `ok`，生成首份服务器备份，容器状态 `healthy`，公网健康接口返回 `ok`。
- 生产依赖检查：`npm audit --omit=dev --registry=https://registry.npmjs.org` 返回0项已知漏洞。已升级图片处理依赖sharp至修复版本。
- 停服备份检查：SQLite `integrity_check` 返回ok，备份所引用的12张作业照片全部存在。
- 本机及局域网地址 `/api/health` 返回ok；尚未用实体手机从外部网络验证连通性。

## 内容质量与使用边界

2026-09-15 视觉改版验证：生产构建通过；`browser-check.js` 重新验证桌面与手机的确认、练习编辑、报告留言输入焦点、分享和家长访问，通过且无运行异常；`upload-ui-check.js` 的压缩、重试、科目切换和批量报告通过。为此次检查使用独立本地预览库，未产生新公网验收账号或消耗模型额度。已修复弹窗延迟初始聚焦覆盖用户已输入焦点的问题。

`check-ui-release.js` 公网检查通过：页面加载本次构建的确切资源文件，1440px、390px、360px 登录及机构注册表单无横向溢出、无浏览器异常，生产体验入口仍禁用。截图及记录位于 `test-results/studio/`；服务器容器 `shiguang:studio-20260915` 状态 healthy。

真实模型流程通过不代表出题或解析绝对正确。人工检查发现过个别不准确的解释，因此产品保留原题复核、练习直接编辑和分享前预览功能。需要用实际机构的手写作业、作文、图形题及教材样本继续评估，不能将合成样例表现作为真实准确率或整班固定耗时承诺。

当前部署为公网单实例机构试用版本；提供机构隔离与老师权限，不含在线收费或微信自动发送。外网 HTTPS IP 入口已在指定服务器运行，部署位置、证书及运维说明见 `DEPLOYMENT.md`。尚未验证实体手机或微信内置浏览器。

## 体验入口

- 公网：https://8.130.50.140 。点击「开通试用」，邀请码见本机 `deploy/ACCESS.local.txt`；无预设账号。
- 电脑：http://localhost:8787
- 同一局域网手机：http://192.168.9.90:8787 （IP变化后需调整，网络须允许访问该端口）
- 页面点击「体验老师工作台」进入独立的虚构数据空间；点击「开通试用」创建自己的机构。
- 重新启动：运行本目录 `start.ps1`；启动与配置细节见 `README.md`。
